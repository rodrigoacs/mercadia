class Card {
  constructor(date, name, set, num, extras, qty, unitPrice, totalPrice) {
    this.date = date
    this.name = name
    this.set = set
    this.num = num
    this.extras = extras
    this.qty = qty
    this.unitPrice = unitPrice
    this.totalPrice = totalPrice
  }
}
module.exports = Card